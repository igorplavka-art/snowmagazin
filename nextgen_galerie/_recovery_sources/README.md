# NextGEN recovery source provenance

These files are durable copies of GitHub Actions discovery artifacts that must not be allowed to expire as the only copy.

- `wayback-gallery-folders.zip`: run `33899806543`, artifact `9947182649` (`snowmagazin-wayback-gallery-folders`). The recovered copy contains 21 folders, 391 indexed image assets, 27 original paths and 364 thumbnails.
- `wayback-article-gallery-map.zip`: run `33900426871`, artifact `9947420879` (`snowmagazin-wayback-article-gallery-map`). It contains article snapshot evidence and candidate NextGEN ID/folder associations.

Mappings are evidence, not truth. A gallery ID is considered verified only when direct page/gallery evidence supports the relation.
